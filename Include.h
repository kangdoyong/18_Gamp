#pragma once

// 이 곳에 프로그램에서 자주 사용되는 헤더파일을 미리 불러놓음
// -> 결과적으로 해당 헤더파일만 부른다면 사용할 여러개의
//    헤더를 한 번에 불러올 수 있음

#include <iostream>
#include <Windows.h>
#include <conio.h>
#include <string>
#include <string.h>
#include <list> // stl 자료구조 중, list
using namespace std;

#include "Define.h"

